//funkction

function myfunc(x, y) {
  return x + y;
}

let output = myfunc(2, 3);

console.log(output);

//const

const new_func = (x, y) => {
  return x - y;
};

output = new_func(2, 10);

console.log(output);
